# Smart Mask acceptance — pending licensed assets

Status: **UNAVAILABLE / NOT RELEASE-APPROVED**

This is the complete report template, not a benchmark result. No model weights or
quality fixtures were fetched, inspected, converted, or approved during SM-16.

## Reproduction record

- Hardware / OS / CPU:
- Python, NumPy, Pillow, ONNX, ONNX Runtime, PyTorch versions:
- Converter revision and opset:
- Candidate source URL, pinned commit, source SHA-256:
- Converted ONNX SHA-256 and byte count:
- Exact seven graph output names, in graph order:
- Fixture-manifest SHA-256 and per-fixture provenance review:
- License evidence and redistribution sign-off:

## Identical bakeoff matrix

Both candidates must use identical approved fixtures, preprocessing, hardware,
runtime, warmup, repetitions, and the provisional 2 px boundary tolerance. Record
aggregate Dice/IoU/boundary-F, category floors, and the full manual edge matrix.

## Performance and resilience

Record cold inference; warm median/p95; preprocessing/postprocessing; 1080p/4K
geometry and compositing; peak/retained RSS; heartbeat; cancellation; and 50-cycle
growth, including raw samples and measurement method.

## Decision

- U2NETP eligible: pending
- Full U2NET eligible: pending
- Manual edge review: pending
- Winner under frozen SM-02 policy: **none**
- User asset/redistribution approval: pending

Thresholds and the 0.03 full-model replacement margin must not be lowered after
observing results.
